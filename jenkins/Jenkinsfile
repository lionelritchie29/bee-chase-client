pipeline {
    agent {
        docker {
            image 'node:lts-bullseye-slim' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('PreInstall') {
          sh 'npm install -g pnpm'
        }

        stage('Build') { 
            steps {
                sh 'pnpm install' 
            }
        }
    }
}