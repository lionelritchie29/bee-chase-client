pipeline {
    agent {
        docker {
            image 'node:lts-bullseye-slim' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('PreInstall') {
          steps {
            sh 'npx pnpm add -g pnpm'
          }
        }

        stage('Build') { 
            steps {
                sh 'pnpm install' 
            }
        }
    }
}